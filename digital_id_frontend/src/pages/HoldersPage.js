import React from 'react';

// PUBLIC_INTERFACE
export default function HoldersPage() {
  return <div>
    <h2 style={{ color: 'var(--button-bg)' }}>Digital ID Holders</h2>
    <p>CRUD for holders. Select to link/unlink unique numbers.</p>
  </div>;
}
